NEW git repo for pdp-automation

try running npm install --legacy-peer-deps if npm install shows peer dependency failure for you 
TODO: Need to change the dependencies according to the peer dependencies in package.json
